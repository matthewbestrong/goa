def find_string_length(string):
    length = len(string)
    print(f"The length of the provided string is {length}.")

# Example usage
user_string = input("Enter a string: ")
find_string_length(user_string)
